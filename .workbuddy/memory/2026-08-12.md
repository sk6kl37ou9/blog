# 2026-08-12 工作日志

## 项目：拾光小筑（个人博客，纯静态）

### 上午：功能增强
- **Markdown 渲染升级**（js/app.js）：新增图片（懒加载）、表格（表头/分隔行）、`###` 三级标题、有序列表、`---` 分隔线、`~~删除线~~`、`*斜体*`；代码块加复制按钮（clipboard + execCommand 降级）；文章页和卡片显示阅读时长估算
- **首页搜索**：实时按标题/摘要/正文/标签过滤，与标签筛选可叠加，筛选时重置分页
- **SEO 补齐**：OG/Twitter/description/canonical meta、内联 SVG favicon（🌿）、防闪白脚本（首屏前应用主题）、新增 404.html
- **RSS/Sitemap**：scripts/build.js 一键生成 feed.xml、sitemap.xml、robots.txt

### 下午：架构升级
- **文章拆分为独立 md 文件**：内容源移到 `posts/*.md`（front matter：title/date/tags/cover/excerpt），站点配置移到 `config.json`，`js/data.js` 改为由 `scripts/build.js` 自动生成（勿手改）
- **写文章流程**：新建 `posts/xxx.md` → `node scripts/build.js`
- **真实封面图**：front matter 的 cover 字段支持图片 URL（自动渲染 `<img>`），emoji 自动回退
- **首页分页**：每页 5 篇（config.json 可调 postsPerPage）
- **归档页** archive.html：按年-月分组展示全部文章，导航加入口

### 晚上：体验优化
- **代码语法高亮**（轻量正则，占位符防二次匹配）：支持 JS/TS/HTML/CSS/Python/Shell，token 配色（关键字/字符串/数字/函数/注释），代码块显示语言标签
- **分享栏**：文章底部微博 / X / 复制链接，复制后提示已复制
- **标签统计**：标签筛选按钮显示每标签文章数，激活态高亮
- **移动端适配**：封面图、分页按钮、分享栏、归档列表响应式调整

### 深夜：十轮连续优化
- **R1 文章目录 TOC**：标题自动生成锚点 id（h2n/h3n 计数 sec-N / sec-N-M），正文上方显示可点击目录，锚点滚动偏移 80px 防吸顶遮挡
- **R2 阅读进度条**：文章页顶部 3px 渐变进度条，随滚动更新
- **R3 搜索高亮**：标题/摘要中关键词 `<mark>` 高亮（正则转义防注入），无结果显示「清除搜索」按钮（事件委托）
- **R4 相关文章**：按共有标签数排序取前 3，显示封面+标题+日期，无相关时隐藏
- **R5 图片灯箱**：正文图片点击放大（ESC/点遮罩关闭，渐入渐出）
- **R6 归档页标签云**：archive.html 顶部标签过滤，点击按标签筛选归档列表，计数联动
- **R7 键盘快捷键**：文章页 ←/→ 上下篇、`T` 切主题、`↑` 返回顶部（输入框/灯箱打开时不触发）
- **R8 字数统计**：文章页与卡片显示「N 字 · 约 M 分钟」（中文按字、英文按词）
- **R9 打印样式**：@media print 隐藏导航/按钮/TOC/分享，正文黑白打印友好
- **R10 性能优化**：图片 loading=lazy + decoding=async，四页 preconnect picsum.photos，脚本 defer

### 深夜续：GitHub + Cloudflare 部署矩阵
- **R11 Giscus 评论**：config.json 新增 giscus 配置（repo/repoId/category/categoryId/mapping），post.html 加评论容器，app.js 动态注入 giscus client.js，主题切换时 postMessage 同步 Giscus 主题
- **R12 GitHub Actions 自动部署**：.github/workflows/deploy.yml（checkout → node20 → node scripts/build.js → cloudflare/wrangler-action@v3 部署 dist），push main 自动发布
- **R13 Cloudflare Pages 配置**：_redirects（404 兜底 + 无后缀路径）、_headers（安全头 + 缓存策略）、wrangler.toml（pages_build_output_dir=dist）
- **R14 部署文档**：DEPLOY.md（域名迁 Cloudflare → 建 GitHub 仓库 → Pages 对接 → 写作流程 → 自定义域名 → Giscus 配置 → API Token 说明），README.md 项目说明
- **R15 动态 SEO meta**：renderPost 动态更新 og:title/og:description/og:url/og:image（封面图），setMeta 工具函数
- **R16 页脚 GitHub 链接**：config.json 加 repository 字段，build.js 带入 SITE，app.js 填充 footer-repo 锚点（四页统一）
- **构建脚本升级**：scripts/build.js 新增 dist/ 部署目录生成（纯静态产物，不含源文件），gitignore 排除

### 深夜续二：体验与工程化
- **R18 PWA**：manifest.json（主题色/图标）、sw.js Service Worker（构建产物网络优先回退缓存、页面缓存优先），脚本 defer 注册，dist 包含 manifest/sw
- **R19 构建压缩**：build.js 加 minifyCSS（去注释+合并空白，dist 的 CSS 从 21KB 压到 17KB），JS 因含模板字符串不做压缩
- **R20 搜索快捷键**：`/` 聚焦搜索框并滚动到可视区，聚焦时 `ESC` 清空搜索并失焦
- **R21 预加载下一篇**：文章页动态插入 `<link rel="prefetch">` 预取下一篇
- **R22 回到目录**：文章页右下角悬浮 📑 按钮（目录滚出视口后显示），点击平滑滚回目录；进度条逻辑复用
- **R23 JSON-LD**：renderPost 注入 Article schema（headline/datePublished/author/publisher/image），动态更新
- **R24 无障碍**：四页加 skip-link 跳转主内容链接，a/button/input 的 :focus-visible 高亮
- **R25 已读标记**：localStorage 记录已读文章 id，首页卡片显示「已读」徽标，进文章自动标记

### 下午：Cloudflare 动态功能（Functions + KV + Pagefind）

- **文章点赞**：functions/api/like.js（POST +1/GET 查询），KV 存储 like:xxx，同 IP 24h 防刷
- **邮件订阅**：functions/api/subscribe.js（POST 订阅/GET /count），KV 存储 sub:邮箱去重
- **联系表单**：functions/api/contact.js，MailChannels 免费发信到 CONTACT_EMAIL，IP 5min 防刷
- **全文搜索**：Pagefind 构建时索引（npx pagefind --site dist），前端优先 Pagefind.search()，降级本地 filter
- **前端更新**：about.html 加订阅框+联系表单，post.html 加喜欢按钮，css/style.css 新增组件样式
- **构建修复**：rmSync(dist) 在 Windows safe-delete 下失败，改为逐文件/逐目录删除
- **配置更新**：wrangler.toml 加 KV binding + CONTACT_EMAIL 变量，_headers 加 /api/* CORS，DEPLOY.md 加 KV 配置指南
- 部署前需 Cloudflare 后台创建 KV namespace 并绑定

### 待办
- config.json 的 url 仍是占位符 https://example.com，部署前换成真实域名
- config.json 的 giscus.repo/repoId/categoryId 与 repository 需填真实 GitHub 仓库信息
- Cloudflare 创建 KV namespace BLOG_KV 并绑定到 Pages 项目
- 设置 CONTACT_EMAIL 环境变量为真实邮箱
- 部署步骤见 DEPLOY.md（域名迁 Cloudflare → GitHub 仓库 → Pages 对接 → 自定义域名）